# Source this file before your status-right definition.
# Add #{E:@worktree_preview_format} once inside your existing status-right value.
set -g @worktree_preview_format '#{?#{@worktree_preview_status},#[fg=green#,bold] #{@worktree_preview_status} #[default],}'
